#include "holberton.h"
/**
 * main - prints alphabet
 *
 * Return: always 0
 */
int print_last_digit(int j)
{
	int last;
	if (j <= 0)
		j = j * -1;
	else
		last = j % 10;
	return _putchar(last + '0');
}
