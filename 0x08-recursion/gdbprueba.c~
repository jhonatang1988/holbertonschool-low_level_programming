#include <stdio.h>
/*
 * main - prueba de gdb and recursion
 *
 */
int print(int nb)
{

	int parcial;
	if(nb < 0)
	{
		return (0);
	}
	printf("%d", nb + print(nb - 1);
	nb--;

	return (nb);
}

int main(void)
{
	print(4);
	return (0);
}
