#include "holberton.h"
/**
 * int is_palindrome - check if is a palindrome
 * @s: string
 *
 * Return: 1 if is palindrome, 0 if not
 */
int is_palindrome(char *s)
{

	char rev[1000];
	int counter = 0;

	if (*s != '\0')
	{
		is_palindrome(s + 1);
		char[]
	}
}
