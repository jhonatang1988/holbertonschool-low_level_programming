#include <stdio.h>
#include <string.h>
/**
 *function to print a string
 *
 * Return: 0 always
 */
int main ()
{
    char str1[50];
    strcpy(str1, "\"Programming is like building a multilingual puzzle");

    puts(str1);

    return(0);
}
