#include <stdio.h>
#include <string.h>
/**
* main - prints with printf
* Return: always 0
*/
int main(void)
{
	char str1[55];

	strcpy(str1, "with proper grammar, but the outcome is a piece of art,");
	printf(str1);

	return(0);
}
